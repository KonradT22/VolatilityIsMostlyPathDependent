# Figure assets

The root [`README.md`](../../README.md#selected-figures) references figures by these filenames. None are currently present — they are generated by `scripts/analysis/build_pdv_inverse_final_figures.py` into a gitignored `benchmarks/` output directory and were never committed.

To surface them in the README, copy small (documentation-sized, PNG preferred over PDF) exports into this directory using exactly these filenames:

- `mahalanobis_81d_ood_separation.png`
- `repricing_rmse_by_maturity.png`
- `fresh_seed_repricing_robustness.png`
- `parameter_recovery_beta0.png`
- `parameter_recovery_beta1.png`
- `parameter_recovery_beta2.png`
- `parameter_recovery_theta1.png`
- `parameter_recovery_theta2.png`

Keep individual files small (a few hundred KB) — this directory is for documentation, not archival copies of full-resolution figures.
